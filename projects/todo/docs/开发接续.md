# 开发接续 — 2026-09-11

## 已确认产品

- 网页版 Todo：不配置 Key 也能添加/编辑/删除/完成任务，支持子任务、日期、优先级、项目和进度。
- 配置自己的 Kimi Key 后，与 AI 商议计划，确认后直接生成待办，通过聊天微调。讨论不写数据，变更预览后应用，可撤销。
- 计划名称用户自定，考研/考公/专升本/私人学习四类作为约束；私人学习以技能成果为目标，期限可选；保留普通日常项目。
- 账号下的任务、计划、聊天、学习记录在电脑和手机同步。按用户隔离，处理并发更新冲突。
- 页面：今天、我的计划、日历、复盘、设置；电脑 AI 侧栏，手机独立对话视图。
- UI 参考 Sunsama 布局、Todoist 清单，局部毛玻璃用于导航/AI 面板/浮层，正文可读，手机和电脑都要完整适配。
- 用户明确选择类似蓝乐的 FastAPI/PostgreSQL/React/TypeScript 与 Docker Compose 部署，后续单独开源。无需题库、社区、排行榜等。

## 本轮中断

用户提醒先切换开发模式。成功执行 `bcdedit /set {current} hypervisorlaunchtype auto` 并复查为 Auto；当前 HypervisorPresent=False，必须重启生效，没有自动重启。vsmlaunchtype=Off 未改动。
Docker Desktop 位于 `D:\DevTools\Docker\DockerDesktop\Docker Desktop.exe`。尝试启动后发现虚拟化未开，已用 `docker desktop stop` 请求正常退出。

## 当前草稿状态

- 原纯静态本地 Todo 开始改成正式应用，旧 dist 文件、server.mjs 和 .openai/hosting.json 已删除，均为本任务此前创建的文件。没有注册或发布 Sites，Docker 架构依用户要求优先。
- package.json、锁文件、Vite/TypeScript 配置、index.html 与 favicon 已生成，npm install 完成。src/main.tsx 与新 UI 尚未编写，不能构建运行。
- backend/db.py、schemas.py、ai.py、main.py 为新后端草稿，包含账号、会话、用户状态、Kimi 设置、聊天草稿、应用/撤销、导出。
- 用户状态带 revision，条件更新防止两端覆盖。AI 变更带基准版本和原状态，应用检查版本，撤销要求无后续修改。
- Key 按用户 Fernet 加密，cookie 为 HttpOnly。默认模型 kimi-k2.6。外部回复先做结构校验，确认后才写入任务。
- 尚未做 Docker Compose、Dockerfile、环境配置、数据库迁移/备份及完整测试；尚未真实联调 Kimi。
- 可用 Python `D:\DevTools\Venvs\blue-music-backend\Scripts\python.exe`，已有相关依赖；新项目最终环境独立部署。

## 重启后继续

1. 检查 HypervisorPresent、Docker，git status/pull，不影响其他改动。
2. 实现新 UI 首个清晰切片并打开预览，再完成全部任务/项目/日历/复盘/设置/AI 功能。
3. 配置独立 PostgreSQL 与 Compose，完成部署/备份说明；核查后端草稿并补必要测试。
4. 验证认证隔离、revision 冲突、AI 校验/应用/撤销/失败恢复，跨浏览器真实同步。
5. 用户已授权复用之前 Kimi Key，尚未读取复制。先读蓝乐记忆入口，再最小范围读取活动配置，仅内存解密，写入新项目专属用户的加密字段。不能让其他账号使用全局 Key。
6. 浏览器验收桌面1440px、手机390px/320px，检查无横向溢出、弹窗/键盘/底栏和AI侧栏可用。
7. 更新准确README，完成检查，commit/push 本项目。

## 相关事实

蓝乐记忆入口：`D:\SunJX\docs\notes\蓝乐_当前上下文速记.md`。
蓝乐 Key 在 ai_provider_configs.api_key_encrypted，解密见 backend/app/core/credential_crypto.py，材料来自私有环境文件；禁止输出明文，不修改蓝乐。
工作区已有 content/blue-music-ai-story 修改/新文件、outputs PPTX 删除、projects/photo-desktop-pet 多文件删除，均不属于本任务，不能暂存提交或回滚。
