# 开发接续 — 2026-09-12

## 已交付第一版

用户确认的基础 Todo + 可选 Kimi、五类自定义计划、账号同步、桌面与手机响应式界面均已实现。完整启动、功能边界、测试与备份见 README。

独立 Compose 项目 yijian：app 4175，PostgreSQL 仅本机 55475，两个容器健康。电脑地址 http://127.0.0.1:4175，同一局域网手机地址 http://192.168.31.48:4175（IP 可能变化）。没有部署公网域名。

用户个人账号 sunjx 已建立，初始登录信息仅存在 `.runtime/account.json`；该目录被 Git 忽略。按用户此前授权，从蓝乐活动配置只读获取 Kimi Key，在内存解密后用本项目密钥加密，仅保存到该用户。模型 kimi-k3，其他账号不共享 Key。禁止将账号文件或密钥复制到文档/提交。

## 验证

- TypeScript/Vite 生产构建通过，Docker 镜像构建完成。
- pytest 5 项：账号隔离、会话退出、CSRF、输入约束、版本冲突、AI 确认/幂等/撤销/过期草稿、Key 不回显、已完成/锁定任务保护。
- PostgreSQL 上浏览器注册、基础模式、两独立上下文同步、手机编辑通过。
- 320/390/768/1440 宽度五页检查无横向溢出，桌面/手机/AI 截图人工检查。
- 真实 Kimi 连接成功；生成一条20分钟草稿，确认后写入，再聊天修改至30分钟通过；专用联调计划已清理。
- 截图仅在 `.runtime/`，测试账号为独立隔离账号。

## 环境恢复

重启后 Hypervisor 已生效。Docker 因 run/dockerInference 和 docker-secrets-engine/engine.sock 临时通信文件残留而报错，停止已失败的 Docker 进程后将对应目录改名留档，重启成功。没有清空 Docker 数据卷。为只读获取 Kimi 配置启动了已有 blue_music_postgres，未改蓝乐业务数据。

## 后续迭代边界

目前是第一版自托管应用，按需 AI 对话推进，没有自动提醒/离线编辑/密码找回。公网部署、独立开源仓库及更细致的长期学习体验留待用户体验后决定。当前已保存到主仓库 projects/todo，不代表已创建独立公开仓库。

保持修改聚焦：content/blue-music-ai-story、outputs、photo-desktop-pet 的未提交修改属于其他任务，不可暂存或回滚。

## AI 管理补充

已将 Key、模型及连接测试移入独立 AI 管理页面，桌面侧栏直接进入，手机从设置进入。增加服务端 `/api/settings/ai/balance`，调用 Kimi 国内站官方余额接口，按账号密钥查询 CNY 可用/现金/代金券余额，不保存余额到业务数据库。前端显示更新时间、手动刷新、未配置和错误状态，更换或移除 Key 会清空旧余额。自动化接口测试增至 10 项。官方契约：https://platform.kimi.com/docs/api/balance 。

## 示例清理与动效

按用户要求移除了示例计划入口及登录演示清单；备份后按固定名称及生成标记清理应用示例计划，用户自建计划不匹配此规则。GSAP 官方 core/react/timeline/performance Skills 已安装到个人技能目录。前端加入 GSAP/useGSAP，局部短过渡支持 prefers-reduced-motion，AI/设置页隐藏新增待办悬浮按钮。第二个 Playwright MCP Skill 按要求搁置。

## 内网穿透 — 2026-09-12

按用户要求启动 Cloudflare Quick Tunnel，公网地址：
https://night-female-floral-vitamin.trycloudflare.com

隧道仅指向 http://127.0.0.1:4175，数据库未通过隧道开放。
已通过公网 HTTPS 验证首页、health、登录、读取账号清单、退出均 HTTP 200。
后台 cloudflared PID 15948；日志 `.runtime/tunnel.err.log`，最新地址 `.runtime/public-url.txt`。
当前网络存在代理 fake-IP DNS；HTTP/2 TLS 握手 EOF，改用真实 edge IP + QUIC 后注册成功。启动命令：

```powershell
& D:\DevTools\Cloudflared\cloudflared.exe tunnel --edge 198.41.192.167:7844 --url http://127.0.0.1:4175 --no-autoupdate --protocol quic
```

本次在隐藏后台进程运行。临时地址会随隧道重新创建改变，没有配置开机启动；电脑、Docker 与隧道需要保持运行。后续固定域名需要命名隧道及域名配置。

## 下拉菜单统一

将 AI 当前计划、快速添加所属计划、任务所属计划/优先级/掌握情况、计划学习类型、日历筛选的原生选择框替换为 Radix Select。自定义圆角、选中状态、菜单避让及滚动，支持键盘/触控；弹窗焦点处理在下拉展开时让出控制，Escape 先关闭下拉。桌面及 320/390 手机菜单、选择保存、键盘 Escape 已验证。

## 墨墨学习跟进

实现独立背单词页、白色未绑定状态、按账号加密 Token、今日进度/词表同步、Kimi 学习分析、任务 vocabulary 类型与进度标签。普通聊天在绑定时附带最新墨墨数据。后端只调用官方只读 study 服务（POST get_study_progress/get_today_items）；任务完成保留用户确认。首次启动新增独立 todo_memo_connections 表。

官方 OpenAPI：https://open.maimemo.com/api_bundle.yaml；study_time 单位毫秒，词表上限1000；当天需打开 App 初始化且开启自动同步，服务仍为公测。没有用户墨墨 Token，不能声称真实个人数据联调通过。12项自动化接口测试通过（包括账户隔离、失败更换保留旧配置、取消绑定保留任务、Kimi上下文不包含墨墨密钥），桌面与320/390px使用模拟供应商响应验收，无演示数据写入真实账号。用户下一步在背单词页面输入自己的墨墨 Token。

未绑定页样式已更新：直接显示紫色简约毛玻璃圆角输入卡片，无需点击文字再展开。左侧背单词入口移至 AI 提示卡下方，与 AI 管理、设置同组；桌面/手机已验证。

## 墨墨绑定错误修复

公网隧道会将后端 502 JSON 替换为 HTML，导致前端显示“服务器暂时无法响应”。墨墨授权失败现返回 422，其余上游失败返回 424，保留具体中文提示；公网无效测试 Token 已验证返回 422 application/json。兼容官方 CLI 使用的 data 包装与直接对象、spelling/voc_spelling 字段；主路径仅在 404 时回退旧 study 路径。已重建 Docker 应用。没有用户真实墨墨 Token，真实绑定和个人进度仍需用户重试验证。
