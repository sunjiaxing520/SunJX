# 蓝乐 AI 音乐创作平台决策记录

## 2026-06-02：首期爬虫数据源

结论：基础版首期先爬 **酷狗热歌榜**。

原因：

- 报价方案中基础版已明确提到酷狗热歌榜。
- SRS 中曾出现 QQ 音乐描述，后续以本决策记录和报价方案为准。
- 后续如进入完整版，再扩展 QQ 音乐、网易云音乐等多源爬取。

开发影响：

- P1/P2 之后的爬虫 Agent 优先实现酷狗热歌榜数据采集。
- 数据模型仍保留 `platform` 字段，为后续多源扩展预留。
- 文档、任务说明和 Cursor 提示词中统一称为“酷狗热歌榜”。

## 2026-06-02：首期创作工具

状态：已被 2026-07-11“官方音乐生成 API”决策取代。

结论：初步暂定使用 **Suno** 作为首期音乐创作工具。

说明：

- 平台不直接承诺“完全原创替代人工作曲”，而是把用户输入和分析结果整理成更适合 Suno 使用的创作素材。
- 对客户讲解时尽量使用中文字段，不直接讲英文接口参数。
- 后续如果 Suno 接口或平台规则变化，应保留创作工具适配层，避免业务逻辑和单一平台强绑定。

首期创作需要准备的中文素材：

```text
歌曲标题
创作类型：带人声 / 纯音乐
歌词：用户填写或 AI 辅助生成
音乐风格：流行、摇滚、电子、民谣、R&B 等
情绪氛围：欢快、悲伤、舒缓、激昂等
节奏速度：慢速 / 中速 / 快速，或 BPM 范围
人声要求：男声 / 女声 / 不指定
补充要求：用户自由描述
```

完整版可扩展：

```text
歌曲结构：主歌 / 副歌 / 过渡段
不想要的风格
多个创作版本
在线试听
版本回退
```

## 2026-06-02：Agent 操作模式

状态：已被 2026-07-11“首期自动工作流和 Agent 分工”决策取代。

结论：首期先做 **人操作多个 Agent**，后续再升级为 **总调度 Agent 调用多个专业 Agent**。

首期模式：

```text
用户分别操作爬虫 Agent、分析 Agent、创作 Agent。
```

后续模式：

```text
用户只提出目标，总调度 Agent 自动拆任务并调用多个专业 Agent。
```

原因：

- 首期先保证客户能看懂、能操作、能验收。
- 人工操作模式开发范围更清晰，风险更低。
- 总调度 Agent 可以作为后续增强能力，不需要推翻现有多个专业 Agent 的设计。

边界说明：

- 首期 Agent 更偏“自动化任务机器人”，例如采集榜单、提交表单、跟踪任务、保存结果。
- 后续增强 Agent 才考虑引入更强的 AI 推理、长期记忆和自动调度能力。
- 自定义工作链、Agent 记忆系统、总调度 Agent 均放入初步设想，不纳入当前首期交付范围。

## 2026-06-02：新增作词 Agent

状态：保留作词 Agent，但基础记忆和多个作词 Agent 调整为后续增强范围。

结论：首期增加 **作词 Agent**。

原因：

- 客户希望用 AI 写歌词。
- Suno 创作带人声歌曲时，歌词是核心素材。
- 作词质量和客户偏好强相关，所以作词 Agent 需要基础记忆。

首期作词 Agent 的能力：

```text
根据主题生成歌词
根据风格生成歌词
根据情绪生成歌词
支持重新生成
支持局部修改副歌/主歌
支持把歌词送入 Suno 创作 Agent
```

首期基础记忆：

```text
用户常用主题
用户偏好的歌词风格
常用表达方式
不想要的词或风格
历史歌词记录
```

边界：

- 首期只做基础偏好记忆，不做复杂长期记忆系统。
- 多个作词 Agent、不同作词 Agent 拥有不同记忆，放入后续增强项目。
- 总调度 Agent 自动选择作词 Agent，也放入后续增强项目。

## 2026-07-11：首期自动工作流和 Agent 分工

结论：首期采用 **一条固定自动工作流**，用户点击一次后，系统按固定顺序自动执行各个 Agent。

```text
酷狗榜单采集
-> 榜单趋势分析
-> AI 作词
-> 官方音乐生成 API
-> 保存结果并展示
```

实现边界：

- 工作流调度器统一控制执行顺序，Agent 之间不直接互相调用。
- 爬虫、分析、作词、音乐生成仍保持独立模块，分别定义清楚输入和输出。
- 首期每种 Agent 只有一个默认实现，不做用户选择多个 Agent。
- 首期不做总调度 AI、自定义工作链、Agent 热插拔和复杂长期记忆。
- 每一步保存状态和结果，失败时记录原因，并允许重新执行。
- 前端使用轮询查看任务状态，首期不增加 WebSocket 和复杂实时系统。

后续扩展时，可以在同一工作流中为不同步骤选择不同的 Agent 实例，但不影响首期固定流程。

## 2026-07-11：内部团队、账号和数据范围

结论：首期是 **单客户内部团队工具**，不是面向公众开放注册的 SaaS。

- 系统不开放用户自助注册。
- 初始化一个超级管理员，由超级管理员创建、停用账号并分配 Agent 使用权限。
- 榜单、分析、歌词、任务和创作结果默认在团队内共享。
- 每条任务和结果仍记录创建人，便于追踪来源。
- 榜单按自然日保存快照，默认保留最近 30 天，页面最多展示最近 15 次运行记录。
- 同一天重复采集时更新当天快照，不伪造额外历史天数。
- 榜单分析默认使用连续 7 天快照；实际不足 7 天时按现有天数分析并降低证据可信度。
- 普通任务产物默认保留 30 天，后续实现时再确定主动保存作品是否长期保留。

## 2026-07-11：官方音乐生成 API

结论：首期不使用非官方 Suno 套壳接口或网页自动化，统一通过官方音乐生成 API 适配层接入。

当前候选顺序：

1. Mureka 官方 API：首选候选，支持中文歌词、完整歌曲、异步任务状态和商业授权。
2. MiniMax Music 官方 API：备选候选，支持中文歌词和完整歌曲生成。

开发要求：

- 在客户提供正式账号前，先用开发测试账号完成同一批中文歌曲对比测试。
- 根据中文咬字、音乐质量、生成时间、水印、价格和稳定性确定最终首期供应商。
- 业务模块只依赖统一的 `MusicProvider` 接口，不在核心流程中写死供应商名称。
- 首期只真正接入一个供应商，不做多供应商自动切换。
- API Key 只保存在后端环境变量中，不进入前端和 Git 仓库。

## 2026-07-19：Suno 官方 Developer API 申请状态

状态：已通过 Suno 官方 Developer API 申请表提交合作申请，当前等待官方审核或补充资料通知；尚未获得 API 文档、访问权限、密钥或商用授权。

- 在收到官方书面回复并完成能力、价格、稳定性和商用条款核验前，不将 Suno 视为已确定供应商，也不据此开始写死 P7 实现。
- 不使用非官方套壳接口、逆向接口或网页自动化替代官方授权。
- 若获得测试资格，优先验证中文歌词表现、异步任务、多个结果、试听/下载、续写能力、并发限制、用量与余额查询及商用许可。
- 若 Suno 未开放或不满足首期验收，继续按既有标准评估 Mureka、MiniMax 等正式官方 API。
- 客户姓名、作品名和可识别信息不进入仓库；Suno 如需客户核验，须先与项目方沟通，由项目方取得客户同意后再提供。

## 2026-07-20：首期音乐供应商锁定为 Suno

结论：项目方确认首期只使用 Suno，前述 Mureka、MiniMax 候选顺序不再作为当前交付路线。仍只允许通过 Suno 官方 API 接入，不使用非官方套壳、逆向接口或网页自动化。

- 已建立 `MusicProvider` 适配层、音乐任务与结果表、音频归档、试听、下载、续写、删除、用量记录和音乐创作页面。
- 线性自动流程扩展为“采集 -> 分析 -> 作词 -> 音乐创作”，使用真实 `lyrics_version_id` 传递到音乐任务。
- 在取得 Suno Platform 正式文档、API Key 和商用权限前，适配器保持明确的 `SUNO_API_NOT_CONFIGURED` 或 `SUNO_API_CONTRACT_PENDING` 失败，不伪造音频和成功状态。
- API Key 只进入后端环境变量，不写入数据库明文、前端、日志、文档或 Git。

## 2026-07-23：分析任务刷新恢复与并发保护

结论：手工分析请求仍由后端完成，但前端发现 `pending/running` 任务后每 2.5 秒刷新任务记录。浏览器刷新不会让页面永久停留在旧的“运行中”状态。

- 后端同一时间只允许一条分析任务；活动任务存在时返回 `ANALYSIS_TASK_ALREADY_RUNNING`，避免刷新后重复点击导致免费模型并发限流。
- 超过供应商完整超时与重试上限的任务仍由统一恢复逻辑标记为 `ANALYSIS_TASK_INTERRUPTED`。
- 智谱返回 `429` 时任务必须真实失败并展示供应商原因，不自动伪装成功。

## 2026-07-13：分析和作词按音乐平台输入反向设计

结论：P5 和 P6 不只输出说明文章，而是共同生成可供正式音乐生成 API 使用的结构化创作方案。

- P5 输出曲风、情绪、主题、场景、速度档位、人声方向、乐器方向、结构和 Hook 建议。
- P6 输出标题、标准段落标签歌词和风格提示词。
- 不从榜单元数据假装推断准确 BPM、调性、和弦或真实编曲。
- P7 只负责把统一创作方案映射成最终供应商参数，避免业务流程绑定单一平台。
- 仓库默认使用明确标记的本地规则基线；正式文本模型通过 OpenAI-compatible 适配器和后端环境变量接入。

## 2026-07-15：开发阶段文本模型

状态：已被 2026-08-11“Kimi 付费稳定性测试”决策取代，智谱配置保留为后备。

结论：本机开发联调使用智谱免费模型 `glm-4.7-flash`，通过现有 OpenAI-compatible 适配器接入。API Key 只保存在被 Git 忽略的本机 `.env`，自动化测试和未配置环境继续使用本地规则基线。正式部署前仍需确认生产账号、额度、稳定性和商用条款。

## 2026-08-11：Kimi 付费稳定性测试

结论：开发阶段从智谱免费模型切换到 Kimi 中国区官方 `kimi-k3`，用于验证付费稳定模型下的分析、作词和自动流程稳定性。

- 使用 Kimi 官方 OpenAI-compatible 地址 `https://api.moonshot.cn/v1`，配置必须先通过低 Token 连接测试再热切换为活动项。
- Kimi K3 使用 `max_completion_tokens`、固定采样参数和 `reasoning_effort=low`；结构化任务继续启用 JSON Mode。
- Dashboard 只向超级管理员调用官方 `/v1/users/me/balance` 展示人民币可用余额；余额查询失败不阻断分析和作词。
- 测试 API Key 只以加密密文存入 PostgreSQL，不写入 `.env`、前端、日志、文档或 Git。
- 该密钥仅用于开发调试。进入客户部署或生产上线准备时，必须先创建独立生产密钥、切换并完成验收，再在 Kimi 控制台吊销当前测试密钥；此项是上线阻断检查，不得遗漏。
- 自动化测试仍使用本地规则或测试替身，不在 CI 中消耗真实 Kimi 额度。

## 2026-07-15：榜单结果详情与音乐文件操作

结论：榜单采集主页面不再常驻展示整张排行榜，改为“折叠的采集结果块 -> 点击结果 -> 在块内展开快照详情”，且一次只展开一个快照。运行记录继续独立保留，用于查看等待、运行、完成、失败和错误原因。

P7 音乐结果必须满足：

- 同一任务返回多个音频时逐首展示，不把多个结果合并成一个下载项。
- 系统建立独立试听区，统一罗列已完成的音乐结果；点击某一首后播放该结果的完整音频。
- 首段初稿和续写结果都是真实生成结果；“试听”只播放已保存音频，不额外请求供应商生成试听版，也不新增一次音乐生成任务。
- 每首音频记录音频地址、下载地址、格式、时长和供应商结果编号。
- 每首音频都有独立下载操作；优先让用户通过浏览器保存文件选择器指定位置，不支持时使用浏览器默认下载。
- 正式音乐供应商若无法提供稳定可访问的试听或下载资源，则不满足首期验收要求。

## 2026-07-15：先试听首段再决定续写

结论：P7 支持“生成首段初稿 -> 进入试听区 -> 用户试听 -> 主动继续续写”的两阶段流程，但不把初稿伪装成供应商提供的特殊试听版。

- 试听不会调用 AI；点击“继续续写”才会创建一次新的外部生成任务并产生新的费用和状态。
- 续写任务必须引用来源结果，保存供应商歌曲编号、续写位置和新增歌词，且不能覆盖原始初稿。
- 原始初稿和续写结果均独立保留、播放和下载。
- Mureka 当前官方 API 明确提供歌曲续写能力，优先进入 P7 实测；是否最终选用仍取决于中文歌曲质量、续写连贯性、价格、稳定性和商用条款。
- 统一 `MusicProvider` 只把续写定义为可选能力；不具备该能力的供应商仍可用于一次性完整歌曲生成。

## 2026-07-15：统一 API 用量账本与余额看板

结论：所有外部 AI 能力共用 `api_usage_records` 调用账本，任务业务表只保存业务状态，不为每个供应商重复增加 Token 字段。

- 每次调用记录任务、操作、供应商、模型、请求方法、接口、供应商请求编号、耗时、重试次数、状态和原始用量摘要。
- 文本模型使用供应商响应中的输入、输出、缓存和总 Token；缺失时保持未知或 0，不使用字符数推算并冒充官方数据。
- 音乐平台按官方返回的积分、生成次数或音频时长记录，Token 只用于真正以 Token 计量的接口。
- Dashboard 展示今日、近 7 日、按供应商汇总和最近调用；旧任务不回填虚构数据。
- 每日用量按日期倒序展示，默认只显示最近 3 天；更早的近 7 日记录保留在页面中，由用户按需展开。
- 自动余额只允许通过供应商官方接口获取。Mureka 文档提供 `/v1/account/billing`；当前智谱兼容接口只接入调用响应用量，余额暂由超级管理员前往控制台查看。
- 余额查询和业务生成解耦，查询失败不能阻断分析、作词或音乐生成任务。

## 2026-07-15：长列表默认收起规则

结论：运行记录、任务记录和接口调用等历史列表不能把全部数据常驻铺开，统一使用“最新记录预览 -> 按需展开 -> 再次收起”的交互。

- 页面级历史记录默认显示最新 5 条，其余记录通过列表底部按钮展开。
- Dashboard 的最近接口调用先按任务类型分为榜单分析、歌词创作、音乐创作和接口测试等折叠组；每次只展开一个组，组内不混入其他任务类型。
- 单个任务内的接口调用明细默认显示最新 3 条，同时保持完整调用顺序可追溯。
- 榜单歌曲、分析选歌等需要连续浏览的数据继续使用分页；榜单快照继续使用单块折叠，不重复套用第二层收起控件。
- 账号等管理对象使用分页，不因默认收起导致用户误以为数据不存在。
- 展开和收起按钮在桌面与手机端使用统一样式，并明确显示尚未展示的记录数量。

## 2026-07-15：文本模型配置热切换

结论：分析和作词不再只依赖进程启动时读取的单组 `AI_*` 环境变量，改为“代码模板 + 数据库连接配置 + 环境变量后备”三层结构。

- 预置本地规则、智谱 BigModel、DeepSeek、阿里百炼 Qwen、MiniMax 和通用 OpenAI-compatible 模板；模型名允许按账号实际能力修改。
- API Key 使用后端主密钥认证加密，数据库只保存密文和末尾提示，任何查询接口都不返回明文。
- 配置必须先完成低 Token 连接测试，测试成功后才能设为唯一活动项；活动项不能直接编辑或删除。
- 分析和作词在每次调用开始时解析活动配置，随后持有该 Provider 完成整次调用。运行中切换只影响后续调用，不重启服务，也不改变已经开始的任务。
- `.env` 保留为首次部署和数据库无活动配置时的后备，可由超级管理员一次性导入；自动化测试继续默认使用本地规则。
- 连接测试本身可能产生费用，因此同样写入 `api_usage_records` 并展示真实 Token。

## 2026-07-15：团队收藏夹与具体产物版本

结论：收藏夹是内部团队共享的精选产物区，首期支持榜单分析报告和歌词版本两类，后续音乐结果沿用同一分类结构扩展。

- 分析收藏指向具体 `analysis_reports.id`，歌词收藏指向具体 `lyrics_versions.id`，不收藏会继续变化的任务外壳。
- 重新生成歌词不会覆盖或替换已经收藏的旧版本。
- 收藏只保存目标类型、目标编号、收藏人、收藏时间和备注；报告与歌词正文继续以原业务表为唯一数据源。
- 每种产物全团队只保留一条收藏关系，重复点击按幂等处理。
- 收藏页展示来源任务、具体版本、供应商、模型、任务 Token、产物生成时间、收藏人和收藏时间，并按类别筛选、按 8 条分页。
- 备注最多 2000 字，默认隐藏，同一时间只展开当前编辑项；移出收藏夹不会删除原始分析或歌词。
- 被收藏产物视为主动保留内容；后续实现 30 天普通产物清理时必须跳过收藏目标，移出收藏后才重新进入普通保留规则。
- 作词任务记录默认显示最新 3 条，保证当前已有少量数据时也能直接看到展开和收起入口。

## 2026-07-15：线性可配置自动工作流

结论：在固定标准流程基础上增加轻量工作流编排。用户可以从当前已实现的采集、分析、作词、音乐创作 Agent 中选择步骤、保存流程模板并一键运行；这次范围调整覆盖原先“首期不做自定义工作链”的旧决定。

- 当前只支持符合数据依赖的线性顺序：采集 -> 分析 -> 作词 -> 音乐创作；用户可以省略不需要的前置步骤，但自动作词必须包含分析，音乐创作必须包含作词。
- 工作流不是只按顺序触发按钮。采集步骤的 `snapshot_id` 用于取出本次歌曲 `entry_ids` 并传给分析；分析步骤的 `report_id` 作为作词的 `analysis_report_id`；作词产出的 `lyrics_version_id` 为 P7 音乐生成预留。
- 流程模板、流程运行和每个步骤运行分别持久化。步骤记录 `task_id`、`output_id`、状态、时间和错误，便于沿数据链定位问题。
- 后台任务先返回运行编号，前端每 2.5 秒轮询；同一时间只允许一条自动流程，避免免费文本接口并发触发限流。
- 分析完成后默认等待 15 秒再开始作词，等待时间由环境变量配置；失败时停在当前步骤，不执行下游，也不产生假成功。
- 步骤权限沿用现有 Agent 权限，模板团队共享；没有对应权限的成员不能创建或运行包含该步骤的流程。
- 音乐创作步骤已经开放并使用歌词版本的统一创作方案传参；未配置 Suno 官方接口时会在该步骤明确失败并停止流程。
- 当前不实现拖拽画布、任意排序、条件分支、并行节点、循环、跨类型参数手工映射或总调度 AI，这些仍属于高级增强范围。

## 2026-07-15：业务任务记录主动删除

结论：采集运行记录、分析记录和作词记录都支持单条和批量永久删除，但按各自数据边界处理关联内容。

- 运行中的任务不能删除；批量删除遇到无效或运行中编号时整批拒绝，避免部分成功造成误判。
- 采集运行记录与每日快照分离。删除 `collection_tasks` 不删除快照；同一天再次采集覆盖当天歌曲并更新最后成功采集时间。
- 删除分析任务时删除报告和分析收藏，既有歌词保留但解除来源报告引用；删除作词任务时删除其全部歌词版本和歌词收藏。
- 用户主动删除收藏产物与未来“自动清理跳过收藏”的规则不同，属于明确确认的永久删除。
- 分析和作词接口用量不随业务产出删除，继续用于 Token、费用和故障审计。
- 自动流程运行历史支持单条和批量删除；只清理流程轨迹及步骤轨迹，不删除流程模板和步骤产出。活动流程不可删除，批量请求保持整批成功或整批失败。
- 删除采集记录后保留快照编号，删除分析或作词产出后清空自动流程步骤中的对应产出编号。

## 2026-07-27：统一 SunoProvider 与受控兼容实现

结论：蓝乐业务层继续只识别 `suno`，外层统一 Provider 可切换
`official` 和 `compatibility`。官方实现为默认和最终交付方向；兼容实现只作为
内网隔离的过渡方案，其他 Agent 和自动流程不得直接调用底层项目。

- 音乐任务改用 Redis 独立 worker，数据库持久化实际实现、尝试次数、下次
  重试和外部任务编号。
- 默认全局并发为 1、请求间隔 30 秒；429、5xx 和网络/生成超时按退避策略
  重试，认证、额度、参数和人机验证错误不盲目重试。
- 兼容合同参考 `gcui-art/suno-api` 的生成、续写、结果和额度接口；Suno
  Cookie 只属于隔离服务，不进入蓝乐数据库、前端、日志或智能体上下文。
- 不集成该项目中的 2Captcha 自动代答、Playwright 指纹规避或验证码绕过。
  hCaptcha 触发时标记 `waiting_human_verification`，管理员在正常网页完成验证
  后手工重新入队。
- 音频统一进入对象存储层，开发默认本地目录，云部署可切 S3-compatible；
  下载器限制 HTTPS、公网地址、重定向、内容类型和最大文件大小。
- Suno Platform 的详细 API 合同当前登录后可见。正式文档到手前官方实现
  必须明确失败，禁止猜测接口路径或伪装联调成功。

## 2026-08-11：Suno 真实额度与员工音乐任务额度分层

结论：系统分别管理供应商真实额度和内部员工任务额度，避免把会随 Suno
计费规则变化的积分直接分拆给员工。

- 超级管理员在音乐创作页查看 Suno 返回的真实剩余额度、本期用量和更新时间；
  普通员工看不到供应商账户总余额。
- 普通员工使用“音乐任务次数”作为内部额度。新生成和续写各扣 `1` 次，任务
  内部重试不重复扣除；超级管理员不限额。
- 新建员工默认额度为 `0`，超级管理员可在账号管理页填写初始额度，并随时把
  员工的当前剩余次数调整到指定值。
- 额度扣减与音乐任务创建处于同一数据库事务，并锁定对应用户记录，避免并发
  请求透支。管理员调整额度时使用同一行锁，避免覆盖同时发生的扣减。
- 任务提交后即占用一次额度。供应商失败、任务删除或结果删除不会自动返还；
  超级管理员确认属于平台故障时，可在账号管理页人工补回。
- `music_quota_used` 记录累计成功提交的生成/续写任务数；管理员调整剩余额度
  不修改累计使用数。任务明细与管理员审计日志共同用于追溯。

## 2026-08-11：创作协作、审核与分级精选

结论：在既有“采集 -> 分析 -> 作词 -> 音乐创作”的主线外，补齐可复用产物的
分级、歌词迭代和可配置审核能力；所有正式产物仍保持版本化，不以对话预览直接
覆盖旧内容。

- 团队收藏夹扩展为 `待分类`、`S`、`A`、`B`、`C`、`D` 六级；每一级都可以
  收藏分析、歌词或音乐结果。分级和备注只属于收藏关系，不复制或改写原始产物。
- 酷狗榜单按 `TOP500` 与 `飙升榜` 分开采集、保存和分析。飙升榜默认采集前 20
  首并允许从单曲直接发起分析；TOP500 的每日快照继续用于多日趋势，不得把两张
  榜单的排名数据混合计算。
- 歌词页提供“AI 助手”多轮修改预览。用户可基于当前歌词描述修改目标，预览可以
  继续对话；只有主动确认预览时才创建新的歌词版本，任何历史版本均不可被覆盖。
- 管理员可通过初始化对话创建多个“审核智能体”，定义人设、评分标准、禁忌和
  输出格式，并为成员分配使用权限。成员只看到记忆概述；完整初始化记录和长期
  记忆只向管理员展开。新增记忆必须由用户显式提交，再由模型归纳去重，不自动
  从日常审核对话写入。
- 音乐创作支持目标风格和避免风格的多选标签，二者不得重叠。当前默认模型为
  `v4.5`，只有超级管理员可调整活动模型；任务在创建时记录模型和标签，之后的
  切换不影响历史或运行中的任务。
- “授权改编”仅面向用户确认拥有必要权利的素材，保存权利确认和来源说明；平台
  不承诺或实现对第三方作品的未授权复制、仿制或规避供应商规则。

## 2026-08-15：分析引用、飙升目标与人工审核闸门

结论：歌词审核作为自动流程中的可选人工闸门，位于歌词创作之后、音乐创作之前。
本决策取代同日早期“未达标后自动修改并连续审核三轮”的设计，系统不得在无人确认时
形成审核循环或继续消耗文本模型 Token。

- 每个审核智能体拥有独立及格线，创建时默认 80 分，管理员可在审核智能体页面调整；
  流程模板只选择审核智能体和可选补充要求，不另存一套及格分。
- 每次进入审核步骤只创建一条正式审核记录。达到该智能体及格线时继续下游；未达到时
  立即进入 `paused`，音乐步骤保持等待，不自动修改、不自动复审。
- 未通过结果必须保存并展示总分、扣分原因和可执行修改建议。用户可明确选择接受当前
  版本继续、打回 AI 按审核意见修改并复审、在歌词页自己修改后复审，或重新生成后复审。
  每次明确操作只开启一次新的审核；若仍不合格，再次暂停等待用户。
- 人工接受必须作为明确决定写入运行详情，不得把低于标准的结果伪装成审核通过。暂停
  期间保留全部上游产出、评分、审核记录和歌词版本链，不重复执行已完成步骤。
- `paused` 流程继续占用全局单流程名额，不受运行超时清理影响，且在用户处理之前不能
  删除，以免丢失待决策上下文。
- 歌词页的分析方向入口按 `待分类 / S / A / B / C / D` 展示全部可用分析与收藏。
  用户界面显示 `AnalysisTask.id`（如“榜单分析 #43”），提交时仍使用内部
  `AnalysisReport.id`；选择方向后自动填充完整创作参数，用户确认修改后再生成。
- 飙升榜表示本次榜单中热度上升最快的歌曲，不计算多日窗口。自动流程允许配置目标
  名次，默认为第 1 名；采集至少覆盖该名次，但分析和后续创作只使用指定的这一首。

## 2026-08-15：手工分析显式选择榜单快照

结论：手工内容分析不再把“全局最后一条采集结果”作为唯一入口。分析页列出保留期内
可用的每日榜单快照，用户必须能够回选任意一份 TOP500 或飙升榜结果。

- 选择器显示榜单名称、榜单日期、歌曲数量和最后成功采集时间，并按最后成功采集时间
  倒序排列；后来采集其他榜单不会遮蔽此前仍在保留期内的结果。
- 分析请求显式提交 `snapshot_id`。用户勾选歌曲时同时提交该快照下的 `entry_ids`；未
  勾选时分析所选快照前 30 首，不得退回全局最新快照。
- 同日同榜再次采集仍覆盖原每日快照并更新 `collected_at`，因此它会回到选择器顶部，
  但不会生成同一天的重复版本。
- 自动流程继续直接传递本次采集步骤的 `snapshot_id -> entry_ids`，不受手工选择器影响。

## 2026-08-16：自动流程在分析完成后确定创作方向

结论：流程模板不再要求用户预选“方向 1 / 方向 2 / 方向 3”。模板配置时分析结果尚未产生，编号没有可供用户判断的业务含义。

- 分析供应商必须将 `creation_directions` 按推荐优先级排序，第一项是最适合本次榜单证据的首选方向。
- 自动流程在分析完成后始终把首选方向传给歌词创作，不读取模板保存的历史 `direction_index`。
- 已有模板中的 `direction_index` 为向后兼容继续允许读取，但不再影响运行结果，不要求数据库迁移。
- 手工作词不受影响：用户仍可先查看各方向的真实内容，再明确选择其中一项。

## 2026-08-16：审核记录内的人工确认式歌词修改

结论：独立审核页面中的每条审核记录都提供常驻在报告顶部的 AI 修改入口。审核正文
与 AI 修改内容可以分别收起；默认只显示 AI 修改摘要标题，展开时才加载对话和输入框。

- 修改请求自动带入该次审核的总结、分数、维度、扣分原因、修改建议和风险提示；
  用户只需补充本轮要求，不必手工复制整份审核报告。
- AI 只返回修改预览，不直接改写正式歌词。用户明确点击“保存并设为当前作品”后，
  系统创建下一个 `LyricsVersion` 并把它标记为当前版本；旧版本继续保留用于恢复和审计。
- 被分配该审核智能体的成员可使用审核记录内的修改能力，不要求额外拥有作词 Agent
  权限；接口仍校验审核记录属于当前智能体，且确认的预览属于该记录所审核的歌词版本，
  防止跨智能体或跨版本保存。
- 对话历史继续写入 `lyrics_assistant_messages`，正式版本继续写入 `lyrics_versions`，
  不新增重复的审核专用歌词表；消息通过可空的 `review_run_id` 区分普通歌词对话和
  每一份审核对话，同一歌词版本的多次审核不得共享或串用预览。
- AI 修改展开后默认只显示最新两条对话，更早记录由用户再次展开；收起后显示已省略
  的消息数量，避免大量审核记录同时铺开输入框和长歌词预览。

## 2026-08-16：统一上游产出选择面板

结论：手工流程中需要引用上一环节产出的入口统一使用“紧凑当前选择 + 大型页内选择
面板”，不再用长文本下拉框承载标题、任务号、摘要和正文。

- 首批覆盖四条手工数据边：榜单快照到分析、分析方向到作词、歌词版本到审核、歌词
  版本到音乐创作。自动流程继续直接传递持久化编号，不打开选择面板，也不改变既有
  调度规则。
- 面板由搜索、业务分组、紧凑结果列表和详情预览组成。列表负责快速定位，预览负责
  核对完整内容；点击列表只改变预览候选，必须再次点击“选择此产出”才写回表单。
- 所有候选都显示可追溯来源链，例如榜单快照号、分析任务号/报告号/方向号、作词
  任务号/歌词版本号。用户看到的业务编号和接口实际提交编号不得混淆。
- “最近产出”展示全部候选；待分类/S/A/B/C/D 是同一批产出的收藏等级视图，不复制
  正式产物。榜单快照另按 TOP500/飙升榜分组。
- 通用交互由 `UpstreamOutputPicker` 管理，各业务页面只负责把领域数据转换成统一展示
  契约并执行确认后的写回。后续新增上游选择入口必须复用该组件，除非交互需求确实不同。

## 2026-08-16：自动流程产出在当前页面连续查看

结论：自动流程运行详情中的步骤产出统一在当前页面抽屉中打开，不再跳转到各业务模块。

- 用户展开一条流程后，可以依次检查采集、分析、歌词、审核和音乐结果；关闭抽屉仍保留
  当前流程位置，不需要返回页面并重新寻找运行记录。
- 抽屉依据每一步持久化的 `task_id`、`output_id` 和 `result_detail` 读取真实产出，不复制
  业务数据，也不改变后端步骤间的参数传递。
- 榜单采集任务和每日快照提供独立的按编号详情接口。采集运行记录删除后，只要快照仍在
  保留期内，自动流程仍可通过 `output_id` 查看当次榜单。
- 审核记录提供按 `agent_id + review_id` 的详情接口并继续执行成员权限校验；历史记录无法
  读取时，流程步骤保存的评分、扣分原因和修改建议作为只读兜底摘要。

## 2026-08-18：Suno 兼容服务恢复 2Captcha 自动人机验证

结论：经用户明确授权，反转此前"不接入 2Captcha 自动代答"的约束，在隔离兼容服务中
恢复上游 `gcui-art/suno-api` 的自动 hCaptcha 处理能力；未配置 2Captcha Key 时仍回退
人工验证流程。

- 背景：实测发现 Suno 对每次生成强制 hCaptcha；此前移除请求头指纹后，生成接口返回
  误导性的"无模型权限"403，恢复上游请求头后确认为真实的 hCaptcha 拦截，人工验证
  无法支撑常态化生成。
- 恢复范围：上游 `getCaptcha()` 完整求解链路（rebrowser-playwright 浏览器、2Captcha
  坐标求解、ghost-cursor 可选）、上游请求头（含客户端标识）和相关依赖；服务继续绑定
  `127.0.0.1`、保留内部 Bearer Token、拒绝入站 Cookie 的隔离边界不变。
- 兜底规则：`TWOCAPTCHA_KEY` 未配置或求解失败时，仍返回
  `SUNO_HUMAN_VERIFICATION_REQUIRED`，走管理员网页验证加重新入队的人工闭环；不得
  因自动求解失败而伪造成功。
- 凭据边界：2Captcha Key 只写入本机 `.env.local`，不进入 Git、日志、聊天或蓝乐数据库；
  健康接口只报告 `captcha_mode`（`auto_solve_2captcha` 或 `human_verification`）。
- 风险确认：用户已知悉该方案依赖非官方私有接口和第三方打码服务，存在账号风控和
  服务条款风险；官方 Suno Platform API 仍是生产首选，到位后切回 `official` 实现。

## 2026-08-25：一键参考创作复用线性工作流

结论：音乐创作页增加独立“一键参考创作”入口。用户只选择参考歌曲；AI 对话输入为
可选项，留空时系统仍自动完成分析、作词和完整音乐生成，不再要求预选方向、歌词、
风格或其他创作参数。

- 当前搜索范围是 30 天保留期内已经采集的酷狗榜单歌曲，按歌名或歌手检索并对同一
  来源歌曲去重；后续全网曲库搜索由独立歌曲搜索 Provider 扩展，不写死到调度层。
- 一键任务创建无模板 `WorkflowRun`，固定包含分析、作词和音乐三个步骤，沿用全局单
  流程约束、Agent 权限、任务额度、Redis 音乐队列、重试、接口用量和对象存储。
- 分析只使用现有榜单元数据及历史趋势证据，自动采用报告首选方向；不得把它描述为
  真实音频拆解。默认要求只借鉴抽象曲风、情绪、节奏、结构和配器方向，不复制来源
  歌曲的歌词、旋律、标志性段落或歌手声线。
- 用户输入的对话要求同时传给作词和音乐步骤；空输入使用默认完整歌曲要求。运行状态
  在音乐页直接显示，页面刷新后从工作流历史恢复最近一次参考流程。
- 当前供应商只接收最终结构化歌词与风格方案。未来官方 Suno API 若提供授权音频上传、
  Cover 或 Remix 契约，应在统一 MusicProvider 增加能力声明，不能让页面或调度层直连。

## 2026-08-31：客户固定歌词结构成为全局创作契约

结论：客户确认的歌词格式不再作为临时补充要求，而是写入文本 Provider 和结构化输出
校验，覆盖手工作词、重新生成、自动流程以及 AI 修改预览。

- 固定 11 段顺序为 `Verse 1 / Verse 2 / Chorus1 / Chorus2 / Interlude / Verse 2 /
  Chorus1 / Chorus2 / Chorus1 / Chorus2 / Outro`，不允许模型自行增加 Intro、Bridge 或
  Pre Chorus。
- `Interlude`、`Outro` 不含歌词；同名重复段落完全一致；两个副歌第一句统一为同一句
  记忆点。以上规则由结构化后处理兜底，避免只依赖模型自觉。
- 全曲统一韵脚作为模型语义约束并要求生成前自检。暂不引入按末字判断的伪校验，后续
  如需机器验韵，应使用经过验收的中文拼音和多音字方案。
- 用户自己的补充要求可以增加题材、风格和表达细节，但不能放宽固定歌词契约。

## 2026-08-31：作词启用团队级隐藏证据记忆

结论：作词 Agent 在每次首次生成、重新生成和 AI 修改前，必须读取团队共享的隐藏
“歌词创作提炼 Skill”。记忆以真实用户输入和用户主动确认的作品为证据，不把模型未确认
的草稿自动当成用户偏好。

- `lyrics_memory_events` 分别保存首次创作需求、修改需求和用户确认结果；首次需求与修改
  需求都带任务、来源歌词和审核指导等上下文，删除业务任务后记忆证据仍保留但解除外键。
- 输入先做控制字符、空白、重复行和无信息短语清理；原始证据继续保留用于追溯，进入模型
  的记忆胶囊只取有限数量和有限长度，防止历史无限增长及 Token 失控。
- 隐藏胶囊固定包含真实创作需求、真实修改需求、需求上下文、已确认结果提炼、榜单歌词
  规律五段。当前任务的明确要求始终优先，历史文本不能覆盖系统规则或固定歌词契约。
- 当前酷狗快照只有歌名、歌手、排名等元数据，没有歌词正文，因此榜单歌词规律段明确标记
  为暂无证据；在取得合法真实歌词语料前，禁止根据标题或排名编造韵脚、句长和金句位置。
- 提炼与正式创作合并在当前模型的一次请求中执行，不额外增加一次 Kimi 调用，避免加倍
  消耗、延迟和 429 风险；这不是多个作词 Agent，也不引入自动调度 Agent。

## 2026-08-31：榜单采集收归超级管理员，成员只读共享结果

结论：榜单结果属于团队共享业务数据，但采集动作、采集运行记录和自动流程中的采集
步骤只允许超级管理员使用。

- 成员导航保留“榜单数据”，直接显示 TOP500 与飙升榜各自最新一份、来源更新时间和
  最后采集完成时间；成员页面不请求采集运行记录，不显示采集、样例或删除操作。
- `POST/GET/DELETE /rankings/collections` 使用超级管理员依赖；`snapshots` 与 `entries`
  继续向全部登录成员开放只读访问。
- 成员权限配置不再提供或接受 `crawler`；已有历史权限不赋予采集能力。
- 自动流程在前端不向成员提供采集步骤，后端也明确拒绝成员创建、运行或恢复包含
  `collection` 的流程，避免绕过榜单页。
- 管理员采集成功后立即刷新当前页面；成员端每 15 秒同步一次最新快照，也可手工刷新。

## 2026-08-31：工作台水印按账号配置

结论：所有已登录页面使用固定覆盖层重复显示斜向水印，水印内容按账号管理，只有超级
管理员能够修改。

- `users.watermark_text` 只保存可选的自定义内容；字段为空时，用户接口统一返回账号名
  作为有效水印，因此旧账号、新账号和恢复默认的账号都不会出现空水印。
- 超级管理员在账号管理页可修改包括自己在内的任意账号水印，最长 50 个字符；清空输入
  表示恢复账号名。成员不能调用水印更新接口。
- 前端在 `AppShell` 使用不可交互的固定 SVG Pattern 覆盖整个视口，水印在滚动时持续
  可见，且不拦截菜单、表单、播放器或弹窗操作。
- 水印用于标识账号来源和降低随意截图传播风险，不视为 DRM、防截屏、数据权限或审计
  的替代方案。

## 2026-08-31：歌词记忆由超级管理员显式治理

结论：隐藏歌词 Skill 继续自动服务每次作词，但新增仅超级管理员可见的“歌词记忆”工作区，
用证据管理、AI 对话调整和命名快照三种方式治理团队长期偏好。

- 原始用户需求、修改要求和确认作品是可追溯证据。管理员可查看详情、停用、恢复或永久
  删除，但 AI 不得改写这些原始证据；管理员固定规则使用独立 `admin_rule` 类型。
- AI 对话采用“生成结构化调整方案 -> 管理员确认 -> 执行”的两阶段流程。生成方案本身
  不改变当前记忆，避免一句含糊对话直接污染所有后续作词。
- 当前隐藏胶囊可保存为命名快照。快照保存当时真正注入模型的内容，仅用于查看和版本
  对照，不自动覆盖当前记忆；每份可改名和删除，最多同时保留 20 份。
- 管理页默认展示摘要和统计，完整输入、上下文及快照正文在点击后按需读取，避免数据增多
  后页面无限拉长。成员没有导航入口，后端管理接口同样强制超级管理员权限。
